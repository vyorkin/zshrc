# vim: set syntax=zsh:

function vpn() {
  case $2 in
    on)
      systemctl start openvpn-home.service
      ;;
    off)
      systemctl stop openvpn-home.service
      ;;
    *)
    echo "${0} {on|off}"
  esac
}
